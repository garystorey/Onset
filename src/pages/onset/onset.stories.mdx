import Onset from "./onset";
import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { BrowserRouter } from "react-router-dom";

export const Template = () => (
  <BrowserRouter>
    <Onset />
  </BrowserRouter>
);

<Meta title="Pages/Onset" />

# Onset

The page is included with the base project. Feel free to remove.
This is here to show an example `storybook` documentation in `MDX`.

<Canvas>
  <Story name="Onset">{Template.bind({})}</Story>
</Canvas>
